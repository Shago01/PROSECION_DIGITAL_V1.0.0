{
  "default": {
    "paths": ["tests/acceptence/**/*.feature"],
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "require": ["tests/acceptance/**/*.ts"],
    "requireModule": ["ts-node/register", "tsconfig-paths/register"]
  }
}
